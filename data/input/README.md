# Input datasets

This directory contains the source CSV extracts used to execute the notebook.

The files were obtained from the publicly available HDB resale-price
datasets on data.gov.sg.

The notebook reads these files using the parameter values defined in Module 1.
